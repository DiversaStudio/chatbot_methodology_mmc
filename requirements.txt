# Plain-pip dependency list, mirroring the runtime dependencies in
# pyproject.toml. Use this if you install with `pip` + `venv` instead of `uv`
# (see the README "Alternative: plain pip + venv" section).
#
# NOTE: torch is intentionally NOT listed here -- install it separately so you
# can pick the CPU or GPU build. See the README.
#
#   pip install "torch>=2.10"     <- CPU, works on any machine, ~200 MB. Use
#                                    this unless you have an NVIDIA GPU.
#   pip install "torch>=2.10" --index-url https://download.pytorch.org/whl/cu128
#                                 <- CUDA 12.8, NVIDIA only, ~3 GB.
#
# Nothing in this repo requires a GPU; it only makes the notebook-03 / pipeline
# NLP faster (~3 min vs ~8 min for a full run on this project's data).

pandas>=2.2
numpy>=1.26
matplotlib>=3.9
seaborn>=0.13
missingno>=0.5
wordcloud>=1.9
geopandas>=1.0
osmnx>=2.0
shapely>=2.1
contextily>=1.7.0
cartopy>=0.24
openpyxl>=3.1
python-docx>=1.1
pyarrow>=24.0.0
emoji>=2.15.0
jupyter>=1.0
jupyterlab>=4.2
ipykernel>=6.29
ipywidgets>=8.1
adjusttext>=1.4.0
bertopic>=0.17.4
sentence-transformers>=5.6.0
umap-learn>=0.5.12
hdbscan>=0.8.44
sentencepiece>=0.2.1
protobuf>=7.35.1
spacy>=3.8

# Spanish spaCy model used by notebook 03 for lemmatization (not on PyPI, so
# it's referenced by its release URL).
es-core-news-sm @ https://github.com/explosion/spacy-models/releases/download/es_core_news_sm-3.8.0/es_core_news_sm-3.8.0-py3-none-any.whl
